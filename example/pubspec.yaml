name: example
description: Examples for animated_size_and_fade.
version: 1.0.0+1

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  http: ^0.12.0+2
  animated_size_and_fade:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  test: ^1.6.3
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
